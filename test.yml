config:
  target: '{{ $processEnvironment.WEB_URL }}'
  phases:
    - duration: 300
      arrivalCount: 100 # 100 virtual users in 5 mins (one virtual user approximately every 3 seconds)

scenarios:
  - flow:
      - get:
          url: '/licence-auth'
          expect:
            statusCode: 200
